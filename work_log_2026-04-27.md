[WORK-LOG] 2026-04-27 — Mass-implementation sweep
================================================================

Scope: implement every action from architecture_audit_2026-04-27.md
plus the three meta-tasks (masterplan refresh, session record,
regression tests). All in one pass. Each item below cites the
file(s) touched + the named symbols added/changed so the user can
mass-validate after.

Format per item:
  [ACTION-N] short title
  Files: <path>:<symbol or line range>
  Summary: 1-2 lines on what changed.
  Validation hint: how to verify it works on the device.

================================================================
SUMMARY TABLE (updated as items land)
================================================================

| ID         | Title                                  | Status   |
|------------|----------------------------------------|----------|
| META-1     | masterplan.md Current State refresh    | DONE     |
| META-2     | conversations/2026-04-26 session log   | DONE     |
| ACTION-9   | Screenshot/snapshot overlay            | DONE     |
| ACTION-8   | Memory pinning + decay surfacing       | DONE     |
| ACTION-1   | Permission-gate skills                 | DONE     |
| ACTION-3   | Biometric gate                         | DONE     |
| ACTION-10  | TLS pinning on HuggingFace download    | DONE     |
| ACTION-6   | Structural prompt-injection defense    | DONE     |
| ACTION-2   | Embedding-based memory recall          | DONE     |
| ACTION-4   | InferenceSession protocol              | DONE     |
| ACTION-5   | Background nudge task                  | DONE     |
| TESTS      | Regression test suite                  | DONE     |
| BUILD      | Final Release build verification       | PASS     |

PHASE-2 BATCH (NEXT-1 through NEXT-10)
| NEXT-1     | Wire MemoryRecallService                | DONE     |
| NEXT-2     | Curated tool catalogue in chatLite      | DONE     |
| NEXT-3     | Rolling token-budget history window     | DONE     |
| NEXT-4     | BGTaskScheduler nudges wired            | DONE     |
| NEXT-5     | Mock InferenceSession + chat E2E tests  | DONE     |
| NEXT-6     | Memory pinning UI                       | DONE     |
| NEXT-7     | Memory conflict detection (logs)        | DONE     |
| NEXT-8     | Decay-report surface in Diagnostics     | DONE     |
| NEXT-9     | Onboarding privacy primer step          | DONE     |
| NEXT-10    | MLXVLM upgrade audit (notes only)       | DONE     |
| BUILD-2    | Post-batch Release build verification   | (running)|


[NEXT-1] Wire MemoryRecallService end-to-end
Files:
  - Eidos/App/AppContainer.swift — instantiates MemoryRecallService,
    passes to MemoryCrystallizer init AND RAGPipeline init,
    bootstrap() launches Task.detached { recall.rebuildIndex() }
  - Eidos/RAG/RAGPipeline.swift — new `memoryRecall: MemoryRecallService?`
    property; chatLite calls recall.recall(query:topK:3, minScore:0.30),
    composes hits into the user-turn <untrusted> block via
    PromptTemplates.sanitizeUntrustedContext
Summary: chatLite now finds relevant memories by MEANING, not
keyword. Wires the dead code from ACTION-2 sweep into the live
chat path. Memory recall metric (recall_hits, recall_chars)
now logged on every turn.
Validation: ask "what did I tell you about food" after
crystallizing a vegetarian fact in a prior chat — should find
it via cosine similarity.

[NEXT-2] Curated tool calling in chatLite
Files:
  - Eidos/Platform/Diagnostics/EidosFeatureFlags.swift —
    new `curatedToolsInChatLite: Bool` flag (default OFF)
  - Eidos/RAG/RAGPipeline.swift::chatLite — when flag ON,
    fetches `availableSkills()` (permission-gated), keeps top
    3, appends a `## Available tools` block to system prompt
    describing the JSON schema; intercepts first chunk of
    Gemma's reply to detect `{"tool":...}` JSON, runs
    `runChatLiteToolHop` to dispatch + re-prompt for natural-
    language confirmation
Summary: re-enables tool execution in chat without going through
the full RAGPipeline.chat path. One tool hop max for thermal
safety. Off by default until users opt in via Diagnostics > Flags.
Validation: enable flag, say "remind me to call mom in an hour";
expected: assistant emits a JSON tool call, dispatch creates a
real reminder via EventKit, follow-up bubble confirms.

[NEXT-3] Rolling token-budget conversation history
Files:
  - Eidos/RAG/RAGPipeline.swift::chatLite — replaced
    `Array(history.suffix(2))` with reversed-iteration over history
    capped at 1500-char budget; dropped turns are signaled to
    Gemma via a one-line "[N earlier turns trimmed]" marker
    (assistant ack pair) so the model knows context is missing
Summary: conversations longer than 2 turns now retain context
up to a token budget. When budget hits, oldest turns drop and
Gemma is told there's older context it can't see (so it doesn't
hallucinate continuity).
Validation: send 6+ messages in one chat; check logs for
`tail_turns: 4-5, dropped_turns: 0` then eventually
`dropped_turns: 1+` once the budget is exceeded.

[NEXT-4] BGTaskScheduler nudges fully wired
Files:
  - project.yml — added `UIBackgroundModes: [processing]` and
    `BGTaskSchedulerPermittedIdentifiers: [com.hissamuddin.eidos.nudges.daily]`
    under target Eidos info.properties
  - Eidos/App/AppContainer.swift::bootstrap — calls
    `NudgeBackgroundTask.register(...)` + `scheduleNext()`
    on iOS only (Mac Catalyst's BGTaskScheduler is a no-op)
Summary: stale active-priority memories (e.g. "follow up with
Anna" 14d untouched) now generate local notifications via the
BG task. iOS picks the actual cadence; we just queue.
Validation: enable Reminders permission, crystallize a fact
into active_priorities, age its lastAccessedAt by 14 days,
foreground+background the app a few times; iOS will eventually
fire the BG task and a nudge notification will appear.

[NEXT-5] Mock InferenceSession + chat E2E tests
Files:
  - EidosTests/MockInferenceSession.swift (new) — implements
    InferenceSession; configurable cannedReply, tokenIntervalMs,
    delayFirstTokenMs, errorAt; records callCount + lastMessages
    for assertions
  - EidosTests/ChatStreamRegressionTests.swift (new) — 6 tests
    covering: ordered streaming, message recording, scripted
    error propagation, consumer cancellation, first-token delay,
    protocol conformance
Summary: future chat regressions can be caught locally in
~1 second via xcodebuild test, instead of an IPA cycle to the
tester. Builds on the InferenceSession protocol from ACTION-4.
Validation: ⌘U in Xcode; all 6 tests should pass on simulator.

[NEXT-6] Memory pinning UI
Files:
  - Eidos/UI/Memory/MemoryEntryDetailView.swift — added "Pin
    forever (skip decay)" / "Unpin (allow decay)" menu item;
    metaBlock shows a "Pinned" label when entry.pinned == true;
    new togglePinned() action calls memoryManager.save with the
    flipped flag
Summary: user-facing half of ACTION-8. Pinning previously had
to be done by hand-editing the .md frontmatter. Now: open a
memory, tap ellipsis menu, tap "Pin forever". Decay engine
already respects the flag.
Validation: open any memory in MemoryBrowserView, pin it,
manually run decay (or wait), confirm priority/tier didn't
change.

[NEXT-7] Memory conflict detection
Files:
  - Eidos/Memory/MemoryRecallService.swift — new
    `similarHighPriority(to:threshold:0.55, maxResults:5)`
    that returns P1/P2 memories whose embedding is close to a
    candidate, used to flag possible contradictions
  - Eidos/Memory/MemoryCrystallizer.swift — after every
    crystallize() pass, for each newly-saved entry: query
    similarHighPriority and emit `crystallizer.conflict-candidate`
    log entry with top match's title + score
Summary: lays the data plumbing for "you said vegetarian
yesterday but said you ate chicken today — which is current?"
reconciliation prompts. Today we ONLY log; the user-facing
prompt is a separate UX deliverable. Logs surface in
Diagnostics > Logs filtered by category=memory.
Validation: crystallize "I'm vegetarian" as P2; in a later
session, crystallize "I had chicken last night"; observe
`memory.crystallizer.conflict-candidate` log entry with high
similarity score.

[NEXT-8] Decay-report surface in DiagnosticsView
Files:
  - Eidos/UI/Settings/DiagnosticsView.swift::MetricsPane — new
    "Memory decay" section that loads
    MemoryDecayEngine.loadLatestReport() and shows demoted /
    archived / evicted / skipped-pinned counts + relative time
    of the last pass
Summary: persists the decay-report data ACTION-8 was already
writing to AppSupport. Users can now SEE memory health from
Settings > Diagnostics > Metrics without reading the JSONL.
Validation: open Diagnostics > Metrics; if a decay pass has
run, the section shows numbers; otherwise a placeholder.

[NEXT-9] Onboarding privacy primer
Files:
  - Eidos/UI/Onboarding/OnboardingView.swift — inserted
    privacyStep between Welcome and Variant Select; three
    icon+headline+body value props (privacy, multimodal,
    memory) + "Continue" pushing to step 2
Summary: explains the on-device + multimodal + memory pitch
BEFORE the user has to wait through a 3.5GB download. Helps
grant rates on later permission prompts (users understand the
why before iOS asks).
Validation: fresh install, walk through onboarding; should
hit Welcome -> Privacy primer -> Variant -> Download -> Home.

[NEXT-10] MLXVLM upgrade audit (notes only)
Files:
  - notes_mlxvlm_upgrade.md (new) — written analysis: the swap
    is upstream-blocked (mlx-swift-lm Issue #389, PRs #180/#185/
    #187 unmerged), our code is forward-compatible, no shipping
    code change is necessary today
Summary: explicitly DEFERRED with rationale. When upstream
gemma4 lands, the upgrade is a Package.resolved bump, not a
refactor. Documented so next session doesn't re-investigate.

DEFERRED:
  ACTION-7 (Lock screen widget) — needs new Xcode target;
  separate ship to avoid breaking the existing build.


================================================================
DETAILED ENTRIES (newest at the bottom as they land)
================================================================

[META-1] masterplan.md Current State refresh
Files: masterplan.md (Current State table at top, ~lines 25-50)
Summary: Updated the Current State table to reflect actual phase
status: Phase 8 shipped, Phase 8.1 (chat-stability v3-v12) shipped,
Phase 8.2 (audit + ACTION queue) in progress. Added "Active
blockers" and "Active engineering posture" sections so a new
contributor reads the master file and immediately knows what's
real vs aspirational.
Validation: open masterplan.md, verify "Phase 8.1" and
"Phase 8.2" rows reference the correct doc files.

[META-2] Session record for the v3-v12 marathon
Files: conversations/2026-04-26_chat_crash_marathon.md (new file)
Summary: Captures the four dead ends, the seven real bugs found
and their fixes, the architectural invariants now codified, and
the decisions locked. Future sessions reading this avoid
re-running our pain.
Validation: read top-to-bottom, confirm every "real bug" row
matches a corresponding [FIX] in developer_log.txt.

[ACTION-9] Privacy snapshot overlay
Files:
  - Eidos/UI/Components/PrivacySnapshotOverlay.swift (new file)
  - Eidos/App/EidosApp.swift::body — added @Environment(\.scenePhase),
    @State isObscured, .overlay { PrivacySnapshotOverlay() } and
    .onChange(of: scenePhase)
Summary: When the app's scenePhase becomes anything other than
.active (background, app switcher, drag-to-split), the entire UI
is covered by an opaque branded overlay. This prevents iOS's
app-switcher snapshot from capturing chat / memory content.
Restored automatically on .active.
Validation: build, open chat with content visible, swipe up to
app switcher — snapshot should show only "Eidos / Private and
on-device" lock screen, not the chat content.

[ACTION-8] Memory pinning + decay-report surfacing
Files:
  - Eidos/Memory/MemoryEntry.swift::MemoryEntry — added
    pinned: Bool (default false)
  - Eidos/Memory/MemoryFrontmatter.swift::render/parse —
    serializes "pinned: true" when set, omits when false (no
    diff churn for existing files)
  - Eidos/Memory/MemoryIndex.swift::MemoryIndexRecord — carries
    pinned through to the in-memory index
  - Eidos/Memory/MemoryDecayEngine.swift::runOnce — pinned
    entries skipped (added to report.skippedPinned), latest
    DecayReport persisted to AppSupport/eidos/memory/decay_latest.json,
    Codable conformance on DecayReport, loadLatestReport() static
    accessor for Diagnostics surface
Summary: Users can mark a memory pinned in the frontmatter
(future UI: a star button in MemoryEntryDetailView). Pinned
entries are skipped by every decay rule. Decay activity is
persisted as JSON so Diagnostics / Settings can show "12
memories archived this week, 3 pinned skipped".
Validation: open .md file in memory store, add `pinned: true`
to frontmatter, run MemoryDecayEngine.runOnce in DEBUG; verify
record appears in skippedPinned and not demoted.

[ACTION-1] Permission-gate skills
Files:
  - Eidos/Skills/SkillProtocol.swift — added SkillAvailability
    enum and Skill.availability() async with default
    .available implementation
  - Eidos/Skills/SkillRegistry.swift — new availableSkills()
    async (parallel TaskGroup); dispatch() re-checks
    availability and returns a clear failure (with permission
    fix instructions) when revoked between catalogue render
    and dispatch
  - Eidos/Skills/Builtin/CalendarSkill.swift — availability()
    returns .permissionDenied when source.hasEventsPermission
    is false
  - Eidos/Skills/Builtin/RemindersSkill.swift — same for
    hasRemindersPermission (RemindersSkill + CreateReminderSkill)
  - Eidos/Skills/Builtin/ContactsSkill.swift — same for
    ContactsSource.hasPermission
  - Eidos/RAG/RAGPipeline.swift::chat — uses await
    buildToolSchemasAvailable() instead of buildToolSchemas
Summary: Tools whose underlying permission is denied are no
longer exposed to Gemma. Stops a class of "the model promised
to do X then it failed because permission was denied" errors.
Validation: deny Reminders permission in Settings, ask Eidos
"what reminders do I have"; expected: chat replies "I can't
read reminders, please grant access in Settings", does NOT
emit a tool-call JSON.

[ACTION-3] Biometric / passcode gate
Files:
  - Eidos/Platform/AppLockController.swift (new file,
    @MainActor @Observable)
  - Eidos/UI/Components/AppLockView.swift (new file, full-
    screen FaceID prompt)
  - Eidos/Platform/Diagnostics/EidosFeatureFlags.swift —
    appLockEnabled flag, default ON in Release
  - Eidos/App/EidosApp.swift — owns the AppLockController,
    bootstraps lock state in init() (cold-start lock), wires
    .fullScreenCover binding, calls handleForegroundTransition
    on scenePhase .active crossing
Summary: On cold start with appLockEnabled, the entire UI is
covered by AppLockView until the user authenticates via
FaceID / TouchID / device passcode. Re-locks after >5 min
backgrounded (configurable in unlockGraceWindow). If the
device has no biometrics AND no passcode, the lock is bypassed
(can't be more strict than device-level security).
Validation: build, force-quit + relaunch — should see lock
screen first; tap Unlock, FaceID prompt; success drops the
cover. Background app for 6 minutes, foreground — lock
re-engages. Disable in Settings -> Diagnostics -> Flags ->
"App lock" to bypass.

[ACTION-10] TLS pinning + hostname allowlist for HF download
Files:
  - Eidos/Inference/SecureHTTPSSession.swift (new file)
    — URLSessionDelegate that:
      1) refuses any TLS handshake whose host isn't on
         SecureHTTPSSession.allowedHosts
      2) validates the system trust chain
      3) optionally compares leaf SPKI SHA-256 against
         pinnedSPKIHashes (empty by default; populate from a
         known-good cert to enable pinning)
  - Eidos/Inference/HuggingFaceDownloader.swift — replaced
    URLSession.shared with `session` lazy var, wired through
    SecureHTTPSSession.session()
Summary: Defense in depth alongside EgressGuard. Even if
EgressGuard is bypassed by a future bug, the model-download
URLSession will refuse to handshake with any host outside the
HF allowlist. SPKI pin slot is wired but empty — populate
when ready (instructions in source comment).
Validation: try changing the URL in HuggingFaceDownloader
to something off-list and observe the
`tls.host.denied` log entry. (Don't actually run a
prod download with a malicious host — just unit-test
the delegate.)

[ACTION-6] Structural prompt-injection defense
Files:
  - Eidos/Inference/PromptTemplates.swift::chat — moved
    retrievedContext OUT of the system message and INTO the
    user turn, wrapped in <untrusted reason="..."> tags;
    removed the prior system-message untrusted block
  - Eidos/Inference/PromptTemplates.swift::sanitizeUntrustedContext
    (new) — strips closing/opening <untrusted>, <system>,
    <|im_start|>/<|im_end|>, [INST]/[/INST], <start_of_turn>,
    case-insensitive, replaces with [redacted-...] markers
Summary: Two-layer defense. (1) Role separation: system prompt
contains only authoritative instructions; retrieved memory +
KB lives in the user turn so attacker-controlled content can't
masquerade as system. (2) Token sanitization: any role-
spoofing or boundary-breaking tokens an attacker may have
embedded are stripped before the prompt reaches Gemma.
Validation: unit-tested in
EidosTests/Phase8_2_RegressionTests.swift::test_chat_template_puts_retrievedContext_in_user_turn
+ test_sanitizeUntrustedContext_*.

[ACTION-2] Embedding-based memory recall
Files:
  - Eidos/Memory/MemoryRecallService.swift (new actor) — wires
    the existing EmbeddingService (Apple NLContextualEmbedding)
    + VectorStore (vDSP cosine) + MemoryManager into a single
    semantic-recall API:
      - indexEntry(_:) — embeds title + body, upserts into
        VectorStore (replaces prior vector for same id)
      - rebuildIndex() — one-shot bootstrap walks every tier
      - forget(entryID:) — removes from vector store
      - recall(query:topK:minScore:) — async [MemoryRecallHit]
        (entry + cosine score), filters archive-tier and
        minScore < 0.30
  - Eidos/Memory/MemoryCrystallizer.swift — recallService:
    optional property + attachRecallService(_:); after every
    crystallize() pass, newly-saved memories are auto-indexed
    via Task.detached so the chat path doesn't wait
Summary: First time Eidos can recall memories by MEANING rather
than keyword. "What did I tell you about thai food?" now finds
a memory titled "User is vegetarian" via semantic similarity.
Validation: run rebuildIndex once at boot, crystallize a
memory like "I love thai food", then ask in a fresh chat
"what cuisines do I like" — should find the memory.
DEFERRED: AppContainer wiring — MemoryRecallService isn't yet
constructed. AppContainer.init must instantiate it (services
already exist) and then call
crystallizer.attachRecallService(recall) so the auto-index
path activates. RAGPipeline.chatLite or ContextBuilder also
needs to call recall.recall(query:) and inject hits into the
retrieved-context block.

[ACTION-4] InferenceSession protocol
Files:
  - Eidos/Inference/InferenceSession.swift (new file) — protocol
    InferenceSession with the same generate() signature
    GemmaSession already has, plus extension GemmaSession:
    InferenceSession {} (zero behavior change today)
Summary: Decouples consumers from the concrete type. Future
backends (Qwen 2.5-VL, Apple Foundation Models, LiteRT-LM)
can drop in without touching RAGPipeline / MemoryCrystallizer
/ DigestGenerator. Also unblocks unit tests by letting them
inject a fake InferenceSession with canned token streams.
Validation: protocol exists; existing call sites still work
because GemmaSession.generate() is unchanged. Future backends
need a single new file conforming to the protocol.

[ACTION-5] Background nudge task
Files:
  - Eidos/Digest/NudgeBackgroundTask.swift (new file, iOS-only
    via #if os(iOS) && !targetEnvironment(macCatalyst)) —
    registers BGTaskScheduler under
    "com.hissamuddin.eidos.nudges.daily", reschedules itself
    every ~4 hours (iOS picks actual cadence), pulls
    ProactiveDigestGenerator.signalsOnly() to harvest stale
    active-priority Nudge structs, dispatches each to
    NotificationScheduler.scheduleNudge
  - Eidos/Platform/NotificationScheduler.swift — added
    scheduleNudge(identifier:title:body:) async
Summary: When the user crystallizes "follow up with Anna" and
14 days pass without that memory being touched, a notification
fires to nudge them. Core piece of the "second brain" UX.
Validation: needs Info.plist entry
BGTaskSchedulerPermittedIdentifiers ->
"com.hissamuddin.eidos.nudges.daily" + Background Modes
capability before iOS will fire it.
DEFERRED: Info.plist + capability changes (deliberately not
in this sweep — separate change with risk to other targets).
Also DEFERRED: NudgeBackgroundTask.register(...) call in
EidosApp.init().

[TESTS] Phase 8.2 regression test suite
Files:
  - EidosTests/Phase8_2_RegressionTests.swift (new file, 11
    tests across ACTION-1, ACTION-6, ACTION-8, ACTION-9,
    ACTION-10) — covers:
      - sanitizeUntrustedContext strips </untrusted>, <untrusted>,
        ChatML role markers, [INST]/[/INST], case-insensitive
      - chat template puts retrievedContext in user turn, NOT
        in system message
      - MemoryEntry.pinned defaults false, round-trips through
        frontmatter render+parse, omitted when false
      - Skill default availability is .available
      - SkillAvailability.permissionDenied is !isAvailable
      - PrivacySnapshotOverlay constructible
      - SecureHTTPSSession.allowedHosts contains huggingface.co
        and excludes arbitrary hosts
Summary: First regression coverage for the chat+safety+memory
stack. Future regressions on these surfaces should fail at
unit-test time, not on tester device.
Validation: ⌘U in Xcode after running xcodegen, or
xcodebuild test -scheme Eidos -destination
'platform=iOS Simulator,name=iPhone 17'.

