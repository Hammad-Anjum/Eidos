package com.hissamuddin.eidos.ui.theme

import androidx.compose.ui.graphics.Color

// Minimal accent palette. Dynamic color (Android 12+) overrides most of this
// at runtime; the fallback scheme picks up the brand blue used in the
// launcher icon foreground (#7DD3FC).

val BrandBlue = Color(0xFF7DD3FC)
val BrandBlueDark = Color(0xFF0284C7)
val Ink = Color(0xFF0B0D12)
val Fog = Color(0xFFF5F7FA)
val Slate = Color(0xFF1F2937)
val Muted = Color(0xFF6B7280)
